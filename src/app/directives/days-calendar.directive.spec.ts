import { DaysCalendarDirective } from './days-calendar.directive';

describe('DaysCalendarDirective', () => {
  it('should create an instance', () => {
    const directive = new DaysCalendarDirective();
    expect(directive).toBeTruthy();
  });
});
